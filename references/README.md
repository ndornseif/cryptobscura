# Reference code
This directory contains reference C implementations of the ciphers,
that are used to compare the rust versions for correctness.

## NIST KATs
The `nist_kat` directory contains the AES competition KATs in
the format specified by NIST.

